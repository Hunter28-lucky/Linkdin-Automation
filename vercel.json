{
  "buildCommand": "cd client && npm install && npm run build && cd .. && cp -r client/build/* .",
  "outputDirectory": ".",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api" }
  ]
}
